import styles from '../styles/Quiz.module.css';

const Header = () => {
  return (
    <header className={styles.header}>
      <h1>Quiz App</h1>
    </header>
  );
};

export default Header;
